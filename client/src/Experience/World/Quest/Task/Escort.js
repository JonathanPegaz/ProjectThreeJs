import Task from "./Task.js";

export default class Escort extends Task {
  constructor(param) {
    super();

    this.init(param)
  }

  init(param) {
    super.init(param)


  }

  destroy() {
    super.destroy();
  }
}